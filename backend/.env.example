# Storage Configuration
# Options: 'local' or 'vercel_blob'
STORAGE_TYPE=local

# Vercel Blob Storage (only needed if STORAGE_TYPE=vercel_blob)
BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Local storage path (only needed if STORAGE_TYPE=local)
DATA_DIR=../data

# API Configuration
PORT=5000
DEBUG=True
